<h1><%= @task_lists.title %> Tasks:</h1>

<ul>
    <% @task_lists.tasks.each do |task| %>
        <li>
            <%= link_to task.title, "/all_tasklists/#{task.id}/#{task.id}" %>
        </li>
    <% end %>
</ul>
